# myeow SwayFX rice — end-4 visual/keybind reference (not a Quickshell port)
# Super = Windows key = Mod4
#
# Layout: this file includes fragments. Installer writes menu.conf + shell.conf.

include $HOME/.config/sway/config.d/*.conf
include $HOME/.config/sway/menu.conf
include $HOME/.config/sway/shell.conf
